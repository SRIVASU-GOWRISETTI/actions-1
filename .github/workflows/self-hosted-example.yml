name: Testing Self-Hosted Runner

on:
  workflow_dispatch:

jobs:
  testing:
    runs-on: [self-hosted, Linux]
    steps:
      - name: Echo content
        run: |
          echo "ok"
          sleep 15s

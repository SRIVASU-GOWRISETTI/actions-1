name: My First Workflow

on: push

jobs:
  first-job:
    runs-on: ubuntu-latest

    steps:
      - name: Welcome message
        run: echo "My first GitHub Actions Job"

      - name: List files
        run: ls

      - name: Read File
        run: cat README.md